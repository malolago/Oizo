[project]
name = "oizo"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "anyio>=4.14.2",
    "dishka>=1.10.1",
    "http-router>=6.0.0",
    "starlette>=1.6.0",
    "uvicorn>=0.52.4",
    "watchfiles>=1.2.0",
]
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/oizo"]
